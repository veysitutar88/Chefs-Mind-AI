# Backend
NODE_ENV=production
PORT=5000
CORS_ORIGIN=http://localhost:3000
RATE_LIMIT=100
SESSION_COOKIE_SECURE=true

# Frontend
NEXT_PUBLIC_API_URL=http://localhost:5001

# === ADDENDUM 2025-11-10 - Dev Ports Configuration ===
# APPEND-ONLY: Существующие секции НЕ РЕДАКТИРУЮТСЯ
# Дата: 2025-11-10 01:58:31 UTC
# Автор: Kilo Code (code mode)

# === PORT CONFIGURATION UPDATE ===
# Backend API: 5003 (стандартизировано)
# Frontend: 3000/3001 
# CORS allowlist: обновлен для поддержки 5003

# === RECOMMENDED PRODUCTION SETTINGS ===
PORT=5003
CORS_ORIGIN=http://localhost:3000,http://localhost:3001
# Frontend API connection (standardized)
NEXT_PUBLIC_API_BASE=http://localhost:5003

# === STARTUP ORDER (APPEND-ONLY) ===
# 1. Database: PostgreSQL (5432) → Ready
# 2. Backend API: 5003 → Health OK
# 3. Frontend: 3000/3001 → Connected

# === LOGGING POLICY (APPEND-ONLY) ===
# ACTION_LOG.md: append-only записи
# Артефакты: reports/artifacts/YYYY-MM-DD/HHMM/

# === NOTES ===
# - PORT=5000 (legacy) в базовой секции сохранен без изменений
# - PORT=5003 (новый стандарт) добавлен в аддендуме
# - NEXT_PUBLIC_API_BASE (стандартизированная переменная фронтенда)
# - Существующие конфигурации остаются неизменными