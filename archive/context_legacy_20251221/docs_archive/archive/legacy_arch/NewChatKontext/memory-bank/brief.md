# Chef’s Mind AI — Brief (Source of Truth)
- Purpose: AI‑driven Kitchen ERP & Media Studio with multi‑agent orchestration (Chef, Accountant, Researcher, Media, QA‑Gate).
- Mode: LOCAL (prod deploy deferred). P1=100% (core layers), P2≈83% (metrics detail + UI common).
- Operating Principle: UNIFIED AI FRAMEWORK v1.1 — Context/Session/Checkpoint + Context Transport (snapshot, session log, manifest, auto‑resume).
- Current Direction: Switch to vertical slices (blocks). Block 0=Infra‑fix (PORT=5001, compose, 501→200 video); next Block 1=MVP User Flow (Login→Dashboard→Chat).
- Decision Log: use Memory Bank as canonical brief/product/context; keep brief updated manually after major merges.
