# Memory Bank Usage (for Kilo Code)

- Treat these files as canonical: brief.md (manual), product.md, context.md, architecture.md, tech.md, tasks.md.
- On each major change:
  1) Update context.md (now/next), tech.md (flags/ports), architecture.md (health payload), tasks.md (if new repeatable steps).
  2) Reply with `[Memory Bank: Active]` and a 5–7 line summary of updates.
- When starting a block:
  - Declare: `Current block: <name>` with Acceptance.
  - Produce minimal change plan, apply, open PR, attach smoke/E2E results.
- Use sources via repo paths (e.g., `@/MASTER_CONTEXT_GUIDE_FULL.md`) for precise updates.

**Status:** Final Sync v2.4 completed. System stabilized.
